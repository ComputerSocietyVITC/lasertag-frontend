export default function TeamPage() {
  return (
    <main className="flex flex-col grow items-center py-6 px-6 bg-gray-50">
      Nothing here, this will be the main page after everything has been
      developed.
    </main>
  );
}
