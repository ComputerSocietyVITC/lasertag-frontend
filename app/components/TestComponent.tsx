const TestComponent = () => {
  return (
    <div className="flex card w-fit">
      This is a test component which uses the styles from globals.css.
    </div>
  );
};

export default TestComponent;
